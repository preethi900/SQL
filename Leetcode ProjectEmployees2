SELECT project_id
FROM Project
GROUP BY project_id
HAVING COUNT(employee_id)=(SELECT count(employee_id)
                            FROM Project
                            GROUP BY project_id
                           ORDER BY COUNT(employee_id) DESC
                           LIMIT 1)
